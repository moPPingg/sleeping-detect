# 📦 GitHub Push Guide - Driver Monitoring System

## 🎯 Những gì NÊN push lên GitHub

### ✅ **SOURCE CODE (Luôn push):**
```
✓ drowsiness_detection_system.py    - Main system
✓ data_collector.py                  - Data collection tool
✓ model_trainer.py                   - Model training script
✓ charts.py                          - Visualization generator
✓ face_mesh_detector.py              - Face detection module
✓ drowsiness_detector.py             - Detection algorithms
✓ glasses_detector.py                - Glasses detection
```

### ✅ **DOCUMENTATION (Luôn push):**
```
✓ README.md                          - Project overview
✓ QUICK_START.md                     - Quick start guide
✓ requirements.txt                   - Dependencies
✓ .gitignore                         - Git ignore rules
```

### ✅ **NOTEBOOKS (Luôn push):**
```
✓ ml_models_comparison.ipynb         - ML comparison notebook
✓ project_documentation.ipynb        - Complete documentation
```

### ✅ **SMALL FILES (Có thể push):**
```
✓ haarcascade_eye_tree_eyeglasses.xml  - OpenCV cascade (22MB)
  → Cần thiết cho glasses detection
  → GitHub cho phép < 100MB
```

---

## ❌ Những gì KHÔNG NÊN push (đã có trong .gitignore)

### **1. Data Files (Quá lớn):**
```
✗ face_data.csv                      - Training data (~50-100MB)
✗ *.csv                              - Any CSV files
```

**Lý do:** 
- Quá lớn cho GitHub (> 100MB)
- Dữ liệu cá nhân, có thể tái tạo
- **Giải pháp:** Hướng dẫn user tự collect data

### **2. Model Files (Quá lớn):**
```
✗ drowsiness_model.pkl               - Trained model
✗ scaler.pkl                         - Scaler object
✗ face_landmarker.task               - MediaPipe model (300MB+)
✗ *.pkl, *.h5, *.pth                 - Any model files
```

**Lý do:**
- Model files rất lớn (> 100MB)
- GitHub có giới hạn 100MB/file
- **Giải pháp:** 
  - User tự train model bằng `model_trainer.py`
  - Hoặc download từ Google Drive (nếu share)

### **3. Generated Files (Có thể tái tạo):**
```
✗ confusion_matrix.png               - Generated by charts.py
✗ data_balance.png                   - Generated by charts.py
✗ feature_importance.png             - Generated by charts.py
✗ Project_Report.html                - Generated from notebook
```

**Lý do:**
- Có thể tái tạo từ code
- Tốn dung lượng không cần thiết
- **Giải pháp:** 
  - User chạy `python charts.py`
  - User export notebook to HTML

### **4. System Files:**
```
✗ __pycache__/                       - Python cache
✗ .ipynb_checkpoints/                - Jupyter checkpoints
✗ *.pyc, *.pyo                       - Compiled Python
✗ .DS_Store, Thumbs.db               - OS files
```

---

## 🚀 Các bước Push lên GitHub

### **Bước 1: Kiểm tra trạng thái**
```bash
git status
```

Bạn sẽ thấy:
- **Green (staged):** Files sẽ được commit
- **Red (unstaged):** Files chưa được add
- **Ignored:** Files trong .gitignore (không hiện)

### **Bước 2: Add files cần thiết**

**Option A: Add tất cả (đã có .gitignore):**
```bash
git add .
```
→ Chỉ add những gì KHÔNG có trong .gitignore

**Option B: Add từng loại:**
```bash
# Add source code
git add *.py

# Add documentation
git add README.md QUICK_START.md requirements.txt

# Add notebooks
git add *.ipynb

# Add cascade file
git add haarcascade_eye_tree_eyeglasses.xml
```

### **Bước 3: Commit với message rõ ràng**
```bash
git commit -m "feat: Complete Driver Monitoring System with ML comparison

- Implement drowsiness detection with Random Forest (97% accuracy)
- Add ML model comparison notebook (6 algorithms)
- Create professional documentation and charts
- Include data collection and training scripts"
```

### **Bước 4: Push lên GitHub**
```bash
git push origin main
```

Hoặc nếu branch khác:
```bash
git push origin master
```

---

## 📊 Kích thước Repository ước tính

### **NẾU push theo .gitignore (Recommended):**
```
Source code:        ~50 KB    ✓
Documentation:      ~30 KB    ✓
Notebooks:          ~500 KB   ✓
Cascade file:       22 MB     ✓
-----------------------------------
TOTAL:              ~23 MB    ✓ Perfect for GitHub!
```

### **NẾU push TẤT CẢ (NOT Recommended):**
```
Above files:        23 MB
face_data.csv:      100 MB    ✗ Too large!
Model files:        150 MB    ✗ Exceeds GitHub limit!
face_landmarker:    300 MB    ✗ Way too large!
Generated images:   1 MB
HTML report:        500 KB
-----------------------------------
TOTAL:              ~575 MB   ✗ WILL FAIL!
```

---

## 🎓 Best Practices cho GitHub

### **1. README.md nên có:**
- ✅ Project description
- ✅ Features list
- ✅ Installation instructions
- ✅ Usage examples
- ✅ Screenshots/charts (if small)
- ✅ **Note về model files:**
  ```markdown
  ## Setup
  
  1. Install dependencies:
     ```bash
     pip install -r requirements.txt
     ```
  
  2. Collect your own data:
     ```bash
     python data_collector.py
     ```
  
  3. Train the model:
     ```bash
     python model_trainer.py
     ```
  
  **Note:** Model files are not included due to size. 
  Train your own model using the provided scripts.
  ```

### **2. Thêm LICENSE:**
```bash
# Add MIT License (most common for open source)
touch LICENSE
```

### **3. Thêm badges (optional):**
Trong README.md:
```markdown
![Python](https://img.shields.io/badge/python-3.8%2B-blue)
![OpenCV](https://img.shields.io/badge/opencv-4.8%2B-green)
![License](https://img.shields.io/badge/license-MIT-blue)
```

---

## 🔒 Nếu cần push Model Files (Large Files)

### **Option 1: Git LFS (Git Large File Storage)**

**Setup Git LFS:**
```bash
git lfs install
git lfs track "*.pkl"
git lfs track "*.task"
git lfs track "face_data.csv"
```

**Commit & Push:**
```bash
git add .gitattributes
git add drowsiness_model.pkl scaler.pkl face_landmarker.task
git commit -m "Add model files with Git LFS"
git push origin main
```

**Lưu ý:** Git LFS có giới hạn bandwidth và storage trên GitHub free tier.

### **Option 2: Google Drive / Dropbox**

**Trong README.md:**
```markdown
## Pre-trained Model

Download pre-trained models from:
- [Google Drive Link](https://drive.google.com/...)
  - `drowsiness_model.pkl` (150 MB)
  - `scaler.pkl` (10 KB)
  - `face_landmarker.task` (300 MB)

Extract to project root directory.
```

### **Option 3: GitHub Releases**

Upload large files as release assets:
1. Go to GitHub repo → Releases
2. Create new release
3. Attach model files (.pkl, .task)
4. Users download from releases page

---

## ✅ Recommended Push Strategy

### **Push lên GitHub (23 MB):**
```
✓ Source code (.py files)
✓ Documentation (.md files)
✓ Notebooks (.ipynb files)
✓ requirements.txt
✓ .gitignore
✓ haarcascade_eye_tree_eyeglasses.xml
```

### **Không push (users tự tạo):**
```
✗ face_data.csv          → Users collect their own data
✗ drowsiness_model.pkl   → Users train their own model
✗ scaler.pkl            → Generated during training
✗ face_landmarker.task  → Download from MediaPipe
✗ *.png images          → Generated by charts.py
✗ *.html reports        → Generated from notebooks
```

### **Hướng dẫn users trong README:**
```markdown
## Quick Start

1. Clone repository:
   ```bash
   git clone https://github.com/yourusername/driver-monitoring-system.git
   cd driver-monitoring-system
   ```

2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

3. Download MediaPipe model:
   - Download `face_landmarker.task` from [MediaPipe](https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task)
   - Place in project root

4. Collect training data:
   ```bash
   python data_collector.py
   ```
   Follow on-screen instructions to collect ~600 samples per class.

5. Train model:
   ```bash
   python model_trainer.py
   ```
   Creates `drowsiness_model.pkl` and `scaler.pkl`.

6. Run system:
   ```bash
   python drowsiness_detection_system.py
   ```
```

---

## 🎯 Final Checklist

### **Trước khi push:**
- [ ] Kiểm tra `.gitignore` đã tạo
- [ ] Xóa các file temporary docs
- [ ] Test `git status` xem files nào sẽ được push
- [ ] Đảm bảo không có sensitive data (passwords, API keys)
- [ ] README.md có hướng dẫn đầy đủ
- [ ] requirements.txt đầy đủ dependencies

### **Sau khi push:**
- [ ] Kiểm tra repo trên GitHub
- [ ] Test clone repo mới về máy khác
- [ ] Test chạy từ đầu (without model files)
- [ ] Update README nếu cần

---

## 📞 Support

Nếu gặp vấn đề:
- GitHub file too large: Remove from git history
  ```bash
  git filter-branch --force --index-filter \
    "git rm --cached --ignore-unmatch filename" \
    --prune-empty --tag-name-filter cat -- --all
  ```

- Git LFS issues: Contact GitHub support or use alternatives

---

<div align="center">

## ✅ READY TO PUSH!

**Recommended: ~23 MB (Clean & Professional)**

Push source code + docs + notebooks only.  
Users train their own models.

**GitHub friendly | Professional | Reproducible**

</div>

---

**Generated:** January 6, 2026  
**For:** Driver Monitoring System  
**Strategy:** Minimal size, maximum reproducibility

